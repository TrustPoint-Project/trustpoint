"""Django apps module which defines the Home app configuration."""

from django.apps import AppConfig


class HomeConfig(AppConfig):
    """Home app configuration."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
